projection;
strict ( 2 );
use draft;

define behavior for ZC_SM30SINGLETON_S //alias <alias_name>
{

   use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  use association _emp { create;  with draft; }
}

define behavior for ZC_DXSM30 //alias <alias_name>
use etag
{
  use update;
  use delete;


  use association _sEmp { with draft; }
}